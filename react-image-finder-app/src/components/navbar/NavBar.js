import React from 'react'
import { Typography } from '@material-ui/core';

const NavBar = () =>
    <div>
        <Typography variant="h5">Image Search App</Typography>
    </div>

export default NavBar
